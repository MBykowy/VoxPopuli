@page
@model VoxPopuli.Pages.Responses.SurveyNotAvailableModel
@{
    ViewData["Title"] = "Survey Not Available";
}

<div class="container py-5">
    <div class="text-center">
        <div class="mb-4">
            <i class="bi bi-exclamation-circle text-warning" style="font-size: 5rem;"></i>
        </div>

        <h1 class="display-5 mb-3">Survey Not Available</h1>

        <p class="lead mb-4">
            @Model.Message
        </p>

        <div class="mt-5">
            <a asp-page="/Index" class="btn btn-primary">
                <i class="bi bi-house-door me-2"></i>Return to Home
            </a>
        </div>
    </div>
</div>